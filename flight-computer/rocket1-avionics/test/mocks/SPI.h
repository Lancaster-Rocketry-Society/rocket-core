// =============================================================================
// test/mocks/SPI.h — SPI mock (the sketch includes it; SD mock bypasses it).
// =============================================================================
#pragma once
struct SPIClass {};
extern SPIClass SPI;
